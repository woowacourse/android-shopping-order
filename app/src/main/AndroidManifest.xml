<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".error.WoowaShoppingApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Shopping"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        <activity
            android:name=".presentation.ui.order.detail.OrderDetailActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.ui.myPage.MyPageActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.ui.serverChoice.ServerChoiceActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".error.ErrorActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.ui.shoppingCart.ShoppingCartActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.ui.productDetail.ProductDetailActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.ui.home.HomeActivity"
            android:exported="false" />
    </application>

</manifest>