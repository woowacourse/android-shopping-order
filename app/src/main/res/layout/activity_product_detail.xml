<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <import type="android.view.View" />

        <variable
            name="product"
            type="woowacourse.shopping.ui.model.Product" />

        <variable
            name="previousProduct"
            type="woowacourse.shopping.ui.model.Product" />

        <variable
            name="addMarketClickListener"
            type="kotlin.jvm.functions.Function0" />

        <variable
            name="previousProductClickListener"
            type="kotlin.jvm.functions.Function0" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".ui.productdetail.ProductDetailActivity">

        <androidx.appcompat.widget.Toolbar
            android:id="@+id/tb_product_detail"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@color/woowa_toolbar_gray"
            android:minHeight="?attr/actionBarSize"
            android:theme="?attr/actionBarTheme"
            app:layout_constraintTop_toTopOf="parent">

            <ImageView
                android:id="@+id/iv_close"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="end"
                android:layout_marginEnd="26dp"
                app:layout_constraintBottom_toBottomOf="@+id/tb_shopping"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintTop_toTopOf="@+id/tb_shopping"
                app:srcCompat="@drawable/ic_close" />

        </androidx.appcompat.widget.Toolbar>

        <ImageView
            android:id="@+id/iv_product"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:scaleType="centerCrop"
            app:image="@{product.imageUrl}"
            app:layout_constraintDimensionRatio="1"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/tb_product_detail"
            tools:srcCompat="@tools:sample/avatars" />

        <TextView
            android:id="@+id/tv_name"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:includeFontPadding="false"
            android:maxLines="1"
            android:padding="18dp"
            android:text="@{product.name}"
            android:textColor="@color/woowa_text_black"
            android:textSize="24sp"
            android:textStyle="bold"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/iv_product"
            tools:text="[든든] 동원 스위트콘" />

        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:background="@color/woowa_light_gray"
            app:layout_constraintTop_toBottomOf="@+id/tv_name"
            tools:layout_editor_absoluteX="-34dp" />

        <TextView
            android:id="@+id/tv_price_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="18dp"
            android:layout_marginTop="16dp"
            android:includeFontPadding="false"
            android:text="@string/tv_price_title"
            android:textColor="@color/woowa_text_black"
            android:textSize="20sp"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/tv_name" />

        <TextView
            android:id="@+id/tv_price"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="18dp"
            android:includeFontPadding="false"
            android:text="@{@string/price_format(product.price.value)}"
            android:textColor="@color/woowa_text_black"
            android:textSize="20sp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="@+id/tv_price_title"
            tools:text="99,800원" />

        <TextView
            android:id="@+id/btn_basket"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:background="@color/woowa_button"
            android:gravity="center"
            android:onClick="@{() -> addMarketClickListener.invoke()}"
            android:paddingVertical="12dp"
            android:text="@string/btn_basket"
            android:textColor="@color/white"
            android:textSize="20sp"
            android:textStyle="bold"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent" />

        <androidx.constraintlayout.widget.ConstraintLayout
            android:id="@+id/layout_previous_product"
            android:layout_width="match_parent"
            android:layout_height="80dp"
            android:layout_marginHorizontal="18dp"
            android:background="@drawable/shape_woowa_light_gray_line_1_rect"
            android:onClick="@{()->previousProductClickListener.invoke()}"
            android:visibility="@{previousProduct == null ?View.GONE :View.VISIBLE}"
            app:layout_constraintBottom_toTopOf="@+id/btn_basket"
            app:layout_constraintTop_toBottomOf="@+id/tv_price_title">

            <TextView
                android:id="@+id/tv_previous_explain"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="18dp"
                android:layout_marginTop="12dp"
                android:includeFontPadding="false"
                android:text="@string/tv_previous_explain"
                android:textColor="@color/woowa_previous_enable"
                android:textSize="12sp"
                android:textStyle="bold"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toTopOf="parent" />

            <TextView
                android:id="@+id/tv_previous_name"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginBottom="12dp"
                android:includeFontPadding="false"
                android:text="@{previousProduct.name}"
                android:textColor="@color/woowa_text_black"
                android:textSize="18sp"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintStart_toStartOf="@+id/tv_previous_explain"
                tools:text="[든든] 동원 스위트콘" />

            <TextView
                android:id="@+id/tv_previous_price"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginEnd="18dp"
                android:includeFontPadding="false"
                android:text="@{@string/price_format(previousProduct.price.value)}"
                android:textColor="@color/woowa_text_black"
                android:textSize="16sp"
                app:layout_constraintBottom_toBottomOf="@+id/tv_previous_name"
                app:layout_constraintEnd_toEndOf="parent"
                tools:text="99,800원" />
        </androidx.constraintlayout.widget.ConstraintLayout>

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
