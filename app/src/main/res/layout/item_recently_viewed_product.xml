<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="recentlyViewedProduct"
            type="woowacourse.shopping.ui.productlist.uistate.RecentlyViewedProductUIState" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="100dp"
        android:layout_height="match_parent"
        android:layout_marginEnd="12dp">

        <ImageView
            android:id="@+id/iv_recently_viewed"
            android:layout_width="0dp"
            android:layout_height="100dp"
            app:layout_constraintDimensionRatio="1"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:urlSrc="@{recentlyViewedProduct.imageUrl}"
            tools:src="@mipmap/ic_launcher_round" />

        <TextView
            android:id="@+id/tv_recently_viewed_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="7dp"
            android:ellipsize="end"
            android:maxLines="1"
            android:text="@{recentlyViewedProduct.name}"
            android:textColor="@color/black"
            android:textStyle="bold"
            app:layout_constraintEnd_toEndOf="@id/iv_recently_viewed"
            app:layout_constraintStart_toStartOf="@id/iv_recently_viewed"
            app:layout_constraintTop_toBottomOf="@id/iv_recently_viewed"
            tools:text="Product Name " />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
