<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="product"
            type="woowacourse.shopping.presentation.product.catalog.ProductUiModel" />

        <variable
            name="handler"
            type="woowacourse.shopping.presentation.product.ProductQuantityHandler" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@drawable/cart_product_count_background"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <androidx.appcompat.widget.AppCompatButton
            android:id="@+id/button_product_minus"
            style="@style/ProductCountMinusButton"
            android:layout_width="@dimen/size_button_square"
            android:layout_height="@dimen/size_button_square"
            android:elevation="0dp"
            android:onClick="@{() -> handler.onMinusQuantity(product)}"
            android:text="@string/text_product_minus"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <TextView
            android:id="@+id/textView_page"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:backgroundTint="@color/white"
            android:paddingHorizontal="16dp"
            android:text="@{String.valueOf(product.quantity)}"
            android:textAlignment="center"
            android:textColor="@color/gray_5"
            android:textSize="22sp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toStartOf="@id/button_product_plus"
            app:layout_constraintStart_toEndOf="@id/button_product_minus"
            app:layout_constraintTop_toTopOf="parent"
            tools:text="1" />

        <androidx.appcompat.widget.AppCompatButton
            android:id="@+id/button_product_plus"
            style="@style/ProductCountPlusButton"
            android:layout_width="@dimen/size_button_square"
            android:layout_height="@dimen/size_button_square"
            android:elevation="0dp"
            android:onClick="@{() -> handler.onPlusQuantity(product)}"
            android:text="@string/text_product_plus"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

    </androidx.constraintlayout.widget.ConstraintLayout>


</layout>