<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <import type="android.view.View" />

        <variable
            name="product"
            type="woowacourse.shopping.product.catalog.ProductUiModel" />

        <variable
            name="catalogHandler"
            type="woowacourse.shopping.product.catalog.event.CatalogEventHandler" />

        <variable
            name="handler"
            type="woowacourse.shopping.product.ProductQuantityHandler" />

    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="6dp"
        android:onClick="@{() -> catalogHandler.onProductClick(product)}"
        >

        <ImageView
            app:loadImage="@{product.imageUrl}"
            android:id="@+id/imageView_product"
            android:layout_width="match_parent"
            android:layout_height="154dp"
            android:contentDescription="@string/text_product_image"
            android:scaleType="centerCrop"
            android:src="@drawable/iced_americano"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <include
            app:product="@{product}"
            app:handler="@{handler}"
            android:visibility="@{product.isExpanded ? View.VISIBLE : View.GONE}"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginBottom="16dp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintBottom_toBottomOf="@id/imageView_product"
            android:id="@+id/layout_quantity_selector"
            layout="@layout/product_count_item" />

        <include
            app:product="@{product}"
            app:handler="@{catalogHandler}"
            android:visibility="@{product.isExpanded ? View.GONE : View.VISIBLE}"
            android:layout_width="53dp"
            android:layout_height="53dp"
            android:layout_marginBottom="8dp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintBottom_toBottomOf="@id/imageView_product"
            android:layout_marginEnd="13dp"
            android:id="@+id/layout_button_open_quantity_selector"
            layout="@layout/product_count_open_item"/>


        <TextView
            android:id="@+id/textView_product_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="6dp"
            android:layout_marginTop="8dp"
            android:ellipsize="end"
            android:maxWidth="139dp"
            android:maxLines="1"
            android:text="@{product.name}"
            android:textColor="@color/black"
            android:textSize="18sp"
            android:textStyle="bold"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/imageView_product"
            tools:text="아이스 카페 아메리카노" />

        <TextView
            android:id="@+id/textView_product_price"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="6dp"
            android:text="@{@string/format_price(product.price)}"
            android:textColor="@color/black"
            android:textSize="16sp"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/textView_product_name"
            tools:text="10,000원" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>