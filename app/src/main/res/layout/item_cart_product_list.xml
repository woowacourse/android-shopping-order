<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="cartProduct"
            type="woowacourse.shopping.common.model.CartProductModel" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginVertical="12dp"
        android:background="@drawable/cart_product_list_background"
        android:paddingHorizontal="13dp"
        android:paddingVertical="10dp">

        <com.google.android.material.checkbox.MaterialCheckBox
            android:id="@+id/cart_product_checkbox"
            android:layout_width="18dp"
            android:layout_height="18dp"
            android:layout_marginEnd="14dp"
            android:buttonTint="@color/primary"
            android:checked="@{cartProduct.checked}"
            app:layout_constraintBottom_toBottomOf="@id/cart_product_list_title"
            app:layout_constraintEnd_toStartOf="@id/cart_product_list_title"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="@id/cart_product_list_title" />

        <TextView
            android:id="@+id/cart_product_list_title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginEnd="7dp"
            android:ellipsize="end"
            android:maxLines="1"
            android:text="@{cartProduct.product.title}"
            android:textColor="#333333"
            android:textSize="18sp"
            android:textStyle="bold"
            app:layout_constraintEnd_toStartOf="@id/cart_product_list_remove_button"
            app:layout_constraintStart_toEndOf="@id/cart_product_checkbox"
            app:layout_constraintTop_toTopOf="parent"
            tools:text="[든든] 동원 스위트콘" />

        <ImageView
            android:id="@+id/cart_product_list_remove_button"
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:layout_marginTop="5dp"
            android:layout_marginEnd="5dp"
            android:background="@drawable/item_remove"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <ImageView
            android:id="@+id/cart_product_list_picture"
            loadPicture="@{cartProduct.product.picture}"
            android:layout_width="136dp"
            android:layout_height="72dp"
            android:layout_marginTop="13dp"
            android:layout_marginBottom="8dp"
            android:scaleType="centerCrop"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/cart_product_list_title"
            tools:src="@drawable/ic_launcher_background" />

        <TextView
            android:id="@+id/cart_product_list_price"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@{@string/product_price(cartProduct.product.price)}"
            android:textColor="#333333"
            android:textSize="16sp"
            app:layout_constraintBottom_toTopOf="@id/cart_product_amount"
            app:layout_constraintEnd_toEndOf="parent"
            tools:text="99,800원" />

        <Button
            android:id="@+id/cart_product_amount_minus_button"
            style="@style/Widget.AppCompat.Button.Borderless"
            android:layout_width="42dp"
            android:layout_height="42dp"
            android:background="@drawable/cart_product_amount_bar_minus_background"
            android:text="@string/minus"
            android:textColor="#555555"
            android:textSize="22sp"
            app:layout_constraintEnd_toStartOf="@id/cart_product_amount"
            app:layout_constraintTop_toTopOf="@id/cart_product_amount" />

        <TextView
            android:id="@+id/cart_product_amount"
            android:layout_width="42dp"
            android:layout_height="42dp"
            android:gravity="center"
            android:text="@{Integer.toString(cartProduct.quantity)}"
            android:textColor="#555555"
            android:textSize="22sp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toStartOf="@id/cart_product_amount_plus_button"
            tools:text="1" />

        <Button
            android:id="@+id/cart_product_amount_plus_button"
            style="@style/Widget.AppCompat.Button.Borderless"
            android:layout_width="42dp"
            android:layout_height="42dp"
            android:background="@drawable/cart_product_amount_bar_plus_background"
            android:text="@string/plus"
            android:textColor="#555555"
            android:textSize="22sp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="@id/cart_product_amount" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>

