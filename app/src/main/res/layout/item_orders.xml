<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="order"
            type="woowacourse.shopping.model.OrderUIModel" />

        <variable
            name="onOrderDetailClick"
            type="kotlin.jvm.functions.Function1" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="18dp">

        <TextView
            android:id="@+id/tv_order_history_date"
            android:layout_width="87dp"
            android:layout_height="27dp"
            android:layout_margin="18dp"
            android:layout_marginTop="13dp"
            android:gravity="center"
            android:text="@{order.orderDate}"
            android:textColor="@color/black"
            android:textSize="16sp"
            android:textStyle="bold"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:text="2023. 5. 27" />

        <TextView
            android:id="@+id/tv_order_detail"
            android:layout_width="79dp"
            android:layout_height="27dp"
            android:layout_margin="18dp"
            android:layout_marginTop="13dp"
            android:background="@drawable/layout_border_radius"
            android:gravity="center"
            android:onClick="@{() -> onOrderDetailClick.invoke(order.id)}"
            android:text="@string/order_history_navigate_to_detail"
            android:textColor="@color/gray_333333"
            android:textSize="16sp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rv_products"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintHorizontal_bias="0.494"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/tv_order_history_date"
            tools:itemCount="3"
            tools:listitem="@layout/item_order" />


    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
