<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:bind="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="cartUiModel"
            type="woowacourse.shopping.ui.cart.CartUiModel" />

        <variable
            name="countButtonClickListener"
            type="woowacourse.shopping.ui.listener.CountButtonClickListener" />

        <variable
            name="cartItemClickListener"
            type="woowacourse.shopping.ui.cart.CartItemClickListener" />

    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="12dp"
        android:layout_marginTop="24dp"
        android:background="@drawable/round">

        <CheckBox
            android:id="@+id/cb_cart_item_check"
            android:layout_width="18dp"
            android:layout_height="18dp"
            android:layout_marginStart="12dp"
            android:layout_marginTop="23dp"
            android:background="@drawable/checkbox"
            android:button="@android:color/transparent"
            android:checked="@{cartUiModel.checked}"
            android:onClick="@{() -> cartItemClickListener.clickCheckBox(cartUiModel.productId)}"
            app:layout_constraintEnd_toStartOf="@id/tv_product_name"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:checked="true" />

        <TextView
            android:id="@+id/tv_product_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:layout_marginTop="18dp"
            android:text="@{cartUiModel.name}"
            android:textColor="@color/gray5"
            android:textSize="18sp"
            android:textStyle="bold"
            app:layout_constraintStart_toEndOf="@id/cb_cart_item_check"
            app:layout_constraintTop_toTopOf="parent"
            tools:text="[든든] 동원 스위트콘" />

        <ImageView
            android:id="@+id/iv_remove"
            android:layout_width="16dp"
            android:layout_height="16dp"
            android:layout_marginTop="8dp"
            android:layout_marginEnd="8dp"
            android:onClick="@{() -> cartItemClickListener.removeCartItem(cartUiModel.productId)}"
            android:scaleType="fitStart"
            android:src="@drawable/close_button"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <ImageView
            android:id="@+id/iv_product_image"
            android:layout_width="136dp"
            android:layout_height="72dp"
            android:layout_marginStart="12dp"
            android:layout_marginTop="20dp"
            android:layout_marginBottom="12dp"
            android:scaleType="fitXY"
            app:imageUrl="@{cartUiModel.imageUrl}"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/tv_product_name"
            tools:src="@drawable/close_button" />

        <include
            layout="@layout/layout_product_count_selection"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="18dp"
            android:layout_marginEnd="14dp"
            android:layout_marginBottom="6dp"
            app:layout_constraintBottom_toTopOf="@id/tv_product_price"
            app:layout_constraintEnd_toEndOf="parent"
            bind:count="@{String.valueOf(cartUiModel.quantity.value)}"
            bind:countButtonClickListener="@{countButtonClickListener}"
            bind:productId="@{cartUiModel.productId}" />

        <TextView
            android:id="@+id/tv_product_price"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="12dp"
            android:layout_marginBottom="17dp"
            android:text="@{@string/product_price(cartUiModel.totalPrice)}"
            android:textColor="@color/gray5"
            android:textSize="16sp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            tools:text="99,800원" />


    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
