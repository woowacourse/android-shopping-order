<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="countHandler"
            type="woowacourse.shopping.presentation.ui.counter.CounterHandler"/>

        <variable
            name="eventHandler"
            type="woowacourse.shopping.presentation.ui.cart.CartEventHandler" />

        <variable
            name="cartItem"
            type="woowacourse.shopping.domain.model.CartItem" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="12dp"
        android:onClick="@{() -> eventHandler.navigateToDetail(cartItem.productId)}"
        android:background="@drawable/shopping_cart_item_gridview">

        <TextView
            android:id="@+id/textView3"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:layout_marginTop="18dp"
            android:text="@{cartItem.productName}"
            android:textColor="@color/gray_500"
            android:textSize="18dp"
            android:textStyle="bold"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:text="상품명" />

        <ImageView
            android:id="@+id/imageView"
            android:layout_width="136dp"
            android:layout_height="72dp"
            android:layout_marginTop="20dp"
            android:scaleType="fitXY"
            app:imageUrl="@{cartItem.imgUrl}"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="@+id/textView3"
            app:layout_constraintTop_toBottomOf="@+id/textView3"
            android:layout_marginBottom="18dp"
            tools:src="@drawable/ic_launcher_background" />

        <include
            android:id="@+id/btn_quantity"
            layout="@layout/cartitem_quantity_selection"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginBottom="4dp"
            app:cartItem="@{cartItem}"
            app:countHandler="@{countHandler}"
            app:layout_constraintBottom_toTopOf="@id/tv_item_cart_price"
            app:layout_constraintEnd_toEndOf="@id/tv_item_cart_price" />

        <TextView
            android:id="@+id/tv_item_cart_price"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="12dp"
            android:textColor="@color/gray_500"
            android:textSize="16dp"
            app:layout_constraintBottom_toBottomOf="@+id/imageView"
            app:layout_constraintEnd_toEndOf="parent"
            app:price="@{cartItem.price*cartItem.quantity}"
            tools:text="10,000원" />

        <ImageView
            android:src="@drawable/ic_back"
            android:layout_width="30dp"
            android:layout_height="30dp"
            android:layout_margin="12dp"
            android:onClick="@{() -> eventHandler.deleteCartItem(cartItem.id)}"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:tint="@color/gray_100" />


    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
