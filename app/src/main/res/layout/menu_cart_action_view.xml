<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>
        <variable
            name="viewModel"
            type="woowacourse.shopping.feature.goods.GoodsViewModel" />
    </data>

    <FrameLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="end|top">

        <ImageView
            android:id="@+id/cart_icon"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:layout_margin="10dp"
            android:layout_gravity="center"
            android:src="@drawable/ic_shopping_cart"
            android:contentDescription="@string/nav_cart_icon_title" />

        <TextView
            android:id="@+id/cart_badge"
            android:layout_width="18dp"
            android:layout_height="18dp"
            android:layout_gravity="end|top"
            android:layout_marginTop="3dp"
            android:layout_marginEnd="3dp"
            android:text="@{String.valueOf(viewModel.totalQuantity)}"
            android:background="@drawable/menu_cart_action_view_background"
            android:gravity="center"
            android:textColor="@color/white"
            app:visible="@{viewModel.totalQuantity > 0}"
            tools:text="3" />
    </FrameLayout>
</layout>