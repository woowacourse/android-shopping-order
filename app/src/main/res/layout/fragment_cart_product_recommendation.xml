<?xml version="1.0" encoding="utf-8"?>
<layout>

    <data>

        <variable
            name="viewmodel"
            type="woowacourse.shopping.view.cart.recommendation.CartProductRecommendationViewModel" />

        <variable
            name="handler"
            type="woowacourse.shopping.view.cart.recommendation.CartProductRecommendationEventHandler" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/white"
        tools:context=".view.cart.recommendation.CartProductRecommendationFragment">


        <TextView
            android:id="@+id/tv_recommend_label"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="16dp"
            android:text="@string/recommend_label"
            android:textColor="@color/black"
            android:textSize="24sp"
            android:textStyle="bold"
            app:layout_constraintBottom_toTopOf="@id/tv_recommend_description"
            app:layout_constraintStart_toStartOf="parent" />

        <TextView
            android:id="@+id/tv_recommend_description"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:layout_marginBottom="30dp"
            android:text="@string/recommend_description"
            app:layout_constraintBottom_toTopOf="@id/rv_recommended_products"
            app:layout_constraintStart_toStartOf="@id/tv_recommend_label" />

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rv_recommended_products"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
            app:layout_constraintBottom_toTopOf="@id/tv_total_price"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <TextView
            android:id="@+id/tv_total_price"
            android:layout_width="0dp"
            android:layout_height="78dp"
            android:background="@color/dark_gray"
            android:gravity="end|center"
            android:padding="12dp"
            android:text="@{String.format(@string/product_price, viewmodel.totalPrice)}"
            android:textColor="@color/white"
            android:textSize="18sp"
            android:textStyle="bold"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toStartOf="@id/btn_order"
            app:layout_constraintStart_toStartOf="parent"
            tools:text="180,000원" />

        <androidx.appcompat.widget.AppCompatButton
            android:id="@+id/btn_order"
            android:layout_width="140dp"
            android:layout_height="78dp"
            android:background="@color/primary_color"
            android:text="@{@string/order(viewmodel.totalCount)}"
            android:textColor="@color/white"
            android:textSize="21sp"
            android:onClick="@{() -> handler.onOrderClick()}"
            android:textStyle="bold"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            tools:text="주문하기(2)" />
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
