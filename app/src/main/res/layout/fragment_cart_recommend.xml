<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <variable
            name="viewModel"
            type="woowacourse.shopping.feature.cart.CartViewModel" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/main"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:fitsSystemWindows="true">

        <TextView
            android:id="@+id/tv_recommend_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="103dp"
            android:layout_marginStart="16dp"
            android:text="@string/fragment_cart_recommend_title"
            android:textColor="@color/black"
            android:textSize="24sp"
            android:textStyle="bold"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent" />

        <TextView
            android:id="@+id/tv_recommend_guide"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/fragment_cart_recommend_guide"
            android:textColor="@color/custom_gray"
            android:textSize="12sp"
            android:textStyle="bold"
            app:layout_constraintTop_toBottomOf="@id/tv_recommend_title"
            app:layout_constraintStart_toStartOf="@id/tv_recommend_title" />

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rv_recommend"
            android:layout_width="match_parent"
            android:layout_height="220dp"
            android:layout_marginTop="29dp"
            app:cartItems="@{viewModel.recommendItems}"
            app:layout_constraintTop_toBottomOf="@id/tv_recommend_guide"
            app:layout_constraintStart_toStartOf="parent" />

        <LinearLayout
            android:id="@+id/total_cart_price"
            android:layout_width="match_parent"
            android:layout_height="78dp"
            android:orientation="horizontal"
            app:layout_constraintBottom_toBottomOf="parent">

            <LinearLayout
                android:layout_width="0dp"
                android:layout_height="match_parent"
                android:layout_weight="2"
                android:background="@color/gray_700">

                <TextView
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_gravity="center_vertical"
                    android:layout_marginEnd="12dp"
                    android:layout_weight="1"
                    android:gravity="right"
                    android:textColor="@color/white"
                    android:text="@{@string/cart_checked_items_price(viewModel.checkedItemsPrice)}"
                    android:textSize="18sp"
                    android:textStyle="bold"
                    tools:text="184,200원" />
            </LinearLayout>

            <TextView
                android:id="@+id/tv_order_button"
                android:layout_width="0dp"
                android:layout_height="match_parent"
                android:layout_weight="1"
                android:background="@color/baemin_mint"
                android:gravity="center"
                android:text="@{@string/cart_order_button(viewModel.totalCheckedItemsCount)}"
                android:textColor="@color/white"
                android:textSize="18sp"
                android:textStyle="bold"
                android:onClick="@{() -> viewModel.orderItems()}"/>
        </LinearLayout>

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>