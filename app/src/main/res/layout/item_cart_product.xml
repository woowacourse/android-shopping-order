<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="cartProduct"
            type="woowacourse.shopping.model.uimodel.CartProductUIModel" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="150dp"
        android:layout_marginHorizontal="20dp"
        android:layout_marginVertical="10dp"
        android:background="@drawable/item_cart_background"
        android:padding="20dp">

        <CheckBox
            android:id="@+id/cb_product"
            android:layout_width="20dp"
            android:layout_height="20dp"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            />

        <TextView
            android:id="@+id/tv_product_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="18sp"
            android:textColor="@color/black"
            android:textStyle="bold"
            android:text="@{cartProduct.productUIModel.name}"
            app:layout_constraintStart_toEndOf="@id/cb_product"
            app:layout_constraintTop_toTopOf="@id/cb_product"
            android:layout_marginStart="10dp"
            app:layout_constraintBottom_toBottomOf="@id/cb_product"
            tools:text="[든든] 동원 콘샐러드" />

        <ImageView
            android:id="@+id/iv_product_image"
            android:layout_width="136dp"
            android:layout_height="72dp"
            imageSrc="@{cartProduct.productUIModel.url}"
            android:layout_marginTop="20dp"
            android:scaleType="centerCrop"
            app:layout_constraintStart_toStartOf="@id/cb_product"
            app:layout_constraintTop_toBottomOf="@id/tv_product_name"
            tools:src="@drawable/ic_launcher_background" />

        <ImageView
            android:id="@+id/iv_cancel"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@drawable/cancel_24"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:tint="#AAAAAA" />

        <TextView
            android:id="@+id/tv_price"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="16sp"
            android:text="@{@string/price_format(cartProduct.productUIModel.price * cartProduct.count.value)}"
            app:layout_constraintBottom_toTopOf="@id/counter_view"
            android:layout_marginBottom="5dp"
            android:textColor="@color/black"
            app:layout_constraintEnd_toEndOf="parent"
            tools:text="10,000원" />

        <woowacourse.shopping.view.customview.CounterView
            android:id="@+id/counter_view"
            android:layout_width="126dp"
            android:layout_height="wrap_content"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            />



    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
