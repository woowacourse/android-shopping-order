<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <variable
            name="coupon"
            type="woowacourse.shopping.domain.model.Coupon" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="112dp"
        android:layout_marginTop="24dp"
        android:layout_marginHorizontal="18dp"
        android:background="@drawable/cart_item_background">

        <CheckBox
            android:id="@+id/cb_coupon_item_select"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="6dp"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"/>

        <TextView
            android:id="@+id/tv_cart_item_name"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginStart="44dp"
            android:layout_marginTop="18dp"
            android:layout_marginEnd="60dp"
            android:ellipsize="end"
            android:maxLines="1"
            android:text="@{String.valueOf(coupon.description)}"
            android:textColor="@color/black"
            android:textSize="18sp"
            android:textStyle="bold"
            app:layout_constraintStart_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:text="5,000원 할인 쿠폰" />

        <TextView
            android:id="@+id/tv_expiration_date"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:layout_marginStart="16dp"
            android:text="@{@string/payment_expiration_date(coupon.expirationDate)}"
            android:textSize="12sp"
            android:textColor="@color/gray_700"
            app:layout_constraintTop_toBottomOf="@id/tv_cart_item_name"
            app:layout_constraintStart_toStartOf="parent"
            tools:text="만료일: 2024년 11월 30일"/>

        <TextView
            android:id="@+id/tv_minimum_order_price"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:layout_marginStart="16dp"
            android:text="@{@string/payment_minimum_order_price(coupon.minimumAmount)}"
            android:textSize="12sp"
            android:textColor="@color/gray_700"
            app:layout_constraintTop_toBottomOf="@id/tv_expiration_date"
            app:layout_constraintStart_toStartOf="parent"
            tools:text="최소 주문 금액: 100,000원"/>
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>