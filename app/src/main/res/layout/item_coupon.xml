<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>
        <variable
            name="coupon"
            type="woowacourse.shopping.domain.model.Coupon" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/couponItemLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:minHeight="72dp"
        android:paddingVertical="12dp"
        android:background="@drawable/coupon_item_background"
        android:layout_marginTop="5dp"
        android:layout_marginBottom="5dp">

        <CheckBox
            android:id="@+id/checkBox_coupon"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:layout_marginTop="4dp"
            android:layout_marginBottom="4dp"
            android:focusable="false"
            android:focusableInTouchMode="false"
            android:clickable="false"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="@+id/tvCouponName"
            app:layout_constraintStart_toStartOf="parent" />

        <TextView
            android:id="@+id/tvCouponName"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="@{coupon.description}"
            android:textSize="16sp"
            android:textStyle="bold"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toEndOf="@id/checkBox_coupon"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintHorizontal_bias="0"
            android:layout_marginStart="8dp"
            android:layout_marginEnd="12dp" />

        <TextView
            android:id="@+id/tvCouponExpiry"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            app:setExpirationDateText = "@{coupon.expirationDate}"
            android:textColor="#888888"
            android:textSize="12sp"
            app:layout_constraintTop_toBottomOf="@+id/tvCouponName"
            app:layout_constraintStart_toStartOf="@+id/tvCouponName"
            app:layout_constraintEnd_toEndOf="parent"
            android:layout_marginTop="4dp"
            android:layout_marginEnd="12dp" />

        <TextView
            android:id="@+id/tvCouponDiscount"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text='@{String.format("최소 주문 금액: %,d원", coupon.minimumAmount != null ? coupon.minimumAmount : 0)}'            android:textSize="14sp"
            app:layout_constraintTop_toBottomOf="@+id/tvCouponExpiry"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="@+id/tvCouponExpiry"
            app:layout_constraintEnd_toEndOf="parent"
            android:layout_marginTop="4dp"
            android:layout_marginBottom="8dp"
            android:layout_marginEnd="12dp" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</layout>