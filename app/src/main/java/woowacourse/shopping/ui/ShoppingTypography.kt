package woowacourse.shopping.ui

import androidx.compose.ui.text.TextStyle
import androidx.compose.ui.text.font.FontWeight
import androidx.compose.ui.unit.sp

object ShoppingTypography {
    val titleMedium =
        TextStyle(
            fontSize = 20.sp,
            fontWeight = FontWeight.W500,
        )

    val detailTitle =
        TextStyle(
            fontSize = 24.sp,
            fontWeight = FontWeight.W700,
        )

    val sectionTitle =
        TextStyle(
            fontSize = 16.sp,
            fontWeight = FontWeight.W700,
        )

    val productName =
        TextStyle(
            fontSize = 18.sp,
            fontWeight = FontWeight.W700,
        )

    val productPrice =
        TextStyle(
            fontSize = 16.sp,
            fontWeight = FontWeight.W400,
        )

    val detailPrice =
        TextStyle(
            fontSize = 20.sp,
            fontWeight = FontWeight.W400,
        )

    val highlightedCaption =
        TextStyle(
            fontSize = 12.sp,
            fontWeight = FontWeight.W700,
        )

    val itemCaption =
        TextStyle(
            fontSize = 12.sp,
            fontWeight = FontWeight.W400,
        )

    val actionLabel =
        TextStyle(
            fontSize = 20.sp,
            fontWeight = FontWeight.W700,
        )

    val paymentAmount =
        TextStyle(
            fontSize = 18.sp,
            fontWeight = FontWeight.W500,
        )
}
